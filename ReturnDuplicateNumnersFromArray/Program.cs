using ReturnDuplicateNumnersFromArray;

var duplicateNumbersArray = new DuplicateNumbersArray();

var list = duplicateNumbersArray.DuplicateNumber();

foreach(var number in list)
{
    Console.WriteLine(number);
}

Console.WriteLine("Hello, World!");
